Terminal 1 — Windows (Backend API)
cd backend
python app.py

Terminal 2 — Windows (Vue frontend)
cd frontend
npm run dev

Terminal 3 — In WSL (Redis)
sudo apt update
sudo apt install redis-server
sudo service redis-server start

Terminal 4 — In WSL (Celery worker)
cd /mnt/c/yourprojectfolder/backend
celery -A tasks worker -l info

Terminal 5 — In WSL (Celery beat)
cd /mnt/c/yourprojectfolder/backend
celery -A tasks beat -l info
